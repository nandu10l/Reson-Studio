# Reson Studio Backend Dependencies

# Web Framework
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
pydantic>=2.0.0
python-dotenv>=1.0.0

# Audio Processing
pydub>=0.25.1
soundfile>=0.12.1
scipy>=1.11.0
numpy>=1.24.0

# Note: pydub requires ffmpeg installed on the system
# Install on Windows: choco install ffmpeg
# Or download from: https://ffmpeg.org/download.html

# Note: Demucs (stem separation) requires Python 3.10
# Install separately with: py -3.10 -m pip install demucs torch torchaudio

# MIDI Processing
mido>=1.2.10

# Audio Analysis (Midify - Spotify's neural pitch detection)
basic-pitch

# AI / ML (used by ai_models/train_lstm.py)
pandas>=2.0.0
tensorflow>=2.15.0

# Lyria RealTime music generation (Gemini API)
google-genai>=1.0.0
websockets>=12.0

# MIDI-LLM — Text-to-MIDI generation (optional, requires CUDA GPU)
# Install separately: pip install transformers accelerate bitsandbytes
# pip install "anticipation @ git+https://github.com/jthickstun/anticipation.git"
transformers>=4.40.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
anticipation @ git+https://github.com/jthickstun/anticipation.git
